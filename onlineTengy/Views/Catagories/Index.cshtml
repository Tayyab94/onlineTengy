@model IEnumerable<onlineTengy.Models.Catagory>

@*@model PagedList.Core.IPagedList<onlineTengy.Models.Catagory>*@

@{
    ViewData["Title"] = "Index";
}

<h2 class="text-capitalize text-justify">Catagories List</h2>


@await Html.PartialAsync("_CreateNewCatagoryButton")

<div class="form-border">

    <table class="table d-xl-table">
        <thead>
            <tr>
                <th>
                    Name
                    @*@Html.DisplayNameFor(model => model.Name)*@
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.DisplayOrder)*@

                    Dispaly Order
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DisplayOrder)
                    </td>

                    @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>*@

                    @await Html.PartialAsync("_TableActionButtons", item.Id)

                </tr>
            }

        </tbody>
    </table>
</div>
